import 'package:flutter/material.dart';

class AfproSplashscreenPage extends StatelessWidget {
  const AfproSplashscreenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello World!'),
      ),
    );
  }
}