import 'package:flutter/material.dart';

class AccessSplashscreenPage extends StatelessWidget {
  const AccessSplashscreenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello World!'),
      ),
    );
  }
}