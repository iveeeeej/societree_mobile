import 'package:flutter/material.dart';

class UsgSplashscreenPage extends StatelessWidget {
  const UsgSplashscreenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello World!'),
      ),
    );
  }
}