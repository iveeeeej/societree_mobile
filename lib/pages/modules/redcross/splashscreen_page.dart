import 'package:flutter/material.dart';

class RedSplashscreenPage extends StatelessWidget {
  const RedSplashscreenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello World!'),
      ),
    );
  }
}