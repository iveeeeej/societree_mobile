import 'package:flutter/material.dart';

class ElecomSplashscreenPage extends StatelessWidget {
  const ElecomSplashscreenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello World!'),
      ),
    );
  }
}